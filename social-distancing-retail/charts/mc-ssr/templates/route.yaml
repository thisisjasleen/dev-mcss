apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: mc-ssr
  namespace: {{ .Values.global.namespace }}
spec:
  to:
    kind: Service
    name: mcss-mc-ssr
  path: /get_all_streams
  port:
    targetPort: 8000
